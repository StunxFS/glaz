# The Glaz Standard Library

The standard library contains useful functions, types, and constants,
as well as containing the Glaz runtime.

> **NOTE:** automatic library loading can be prevented with the
`--no-std` option.
